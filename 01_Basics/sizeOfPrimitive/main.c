#include <stdio.h>

void main()
{
    printf("size of char in bytes= %zu\n",sizeof(char));
    printf("size of int in bytes = %zu\n", sizeof(int));
    printf("size of float in bytes= %zu\n", sizeof(float));
    printf("size of double in bytes = %zu", sizeof(double));
}
