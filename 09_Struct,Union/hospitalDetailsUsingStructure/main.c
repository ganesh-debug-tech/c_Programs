#include <stdio.h>
#include <string.h>

struct hospital
{
    int id;
    char name[50];
    char disease[50];
};

void main()
{
    struct hospital h;
    printf("Enter the patient id : ");
    scanf("%d",&h.id);
    printf("Enter the patient name : ");
    scanf("%s",h.name);
    printf("Enter the patient disease : ");
    scanf("%s",&);

}
